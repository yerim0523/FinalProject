<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.toleisure.mybatis.dao.IMainDAO">

	
	<select id="AllGroupList" resultType="com.toleisure.mybatis.dto.GroupDTO">
   		SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_NAME AS memName, CATE1 AS grCate1Name, CATE2 AS grCate2Name
		FROM ALL_GROUP_VIEW
	</select>
	
	<select id="CultureGroupList" resultType="com.toleisure.mybatis.dto.GroupDTO">
   		SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_NAME AS memName, CATE1 AS grCate1Name, CATE2 AS grCate2Name
		FROM CULTURE_GROUP_VIEW
	</select>
	
	<select id="ExhibitGroupList" resultType="com.toleisure.mybatis.dto.GroupDTO">
		SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_NAME AS memName, CATE1 AS grCate1Name, CATE2 AS grCate2Name
		FROM CUL_EXHIBITION_VIEW
	</select>
	
	<select id="PlayGroupList" resultType="com.toleisure.mybatis.dto.GroupDTO">
		SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_NAME AS memName, CATE1 AS grCate1Name, CATE2 AS grCate2Name
		FROM CUL_PLAY_VIEW
	</select>
	
	<select id="ShowGroupList" resultType="com.toleisure.mybatis.dto.GroupDTO">
		SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_NAME AS memName, CATE1 AS grCate1Name, CATE2 AS grCate2Name
		FROM CUL_SHOW_VIEW 
	</select>
	
	<!-- ============================================음식점 DAO================================================ -->
	<select id="FoodGroupList" resultType="com.toleisure.mybatis.dto.GroupDTO">
   		SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_NAME AS memName, CATE1 AS grCate1Name, CATE2 AS grCate2Name
		FROM FOOD_GROUP_VIEW
	</select>
	
	<select id="CafeGroupList" resultType="com.toleisure.mybatis.dto.GroupDTO">
   		SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_NAME AS memName, CATE1 AS grCate1Name, CATE2 AS grCate2Name
		FROM FOOD_CAFE_VIEW
	</select>
	
	<select id="RestaurantGroupList" resultType="com.toleisure.mybatis.dto.GroupDTO">
   		SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_NAME AS memName, CATE1 AS grCate1Name, CATE2 AS grCate2Name
		FROM FOOD_RESTAURANT_VIEW
	</select>
	
	<!-- ============================================음식점 DAO END================================================ -->
	
	<!-- ============================================테마별 DAO================================================ -->
	<select id ="HotGroupList" resultType="com.toleisure.mybatis.dto.GroupDTO">
		SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_NAME AS memName, CATE1 AS grCate1Name, CATE2 AS grCate2Name
		FROM HOT_GROUP_VIEW
		
		SELECT AVG(R.REVIEW_STAR) AS grStarAvg
		FROM NOW_GROUP NG JOIN TBL_GROUP G
		ON NG.GR_CODE = G.GR_CODE
		    JOIN FIXED_GROUP FG
		    ON NG.NG_CODE=FG.NG_CODE
		        JOIN GR_JOIN GJ
		        ON FG.FIX_CODE=GJ.FIX_CODE
		            JOIN REVIEW R
		            ON R.JOIN_CODE=GJ.JOIN_CODE
		WHERE G.GR_CODE = #{starCode}
	</select> 
	
	<select id ="HotGroupListAll" resultType="com.toleisure.mybatis.dto.GroupDTO">
		SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_NAME AS memName, CATE1 AS grCate1Name, CATE2 AS grCate2Name
		FROM HOT_GROUP_ALLVIEW
	</select>
		
		
	<select id ="NewGroupList" resultType="com.toleisure.mybatis.dto.GroupDTO">
		SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_NAME AS memName, CATE1 AS grCate1Name, CATE2 AS grCate2Name
		FROM NEWEST_GROUP_VIEW
		
		SELECT AVG(R.REVIEW_STAR) AS grStarAvg
		FROM NOW_GROUP NG JOIN TBL_GROUP G
		ON NG.GR_CODE = G.GR_CODE
		    JOIN FIXED_GROUP FG
		    ON NG.NG_CODE=FG.NG_CODE
		        JOIN GR_JOIN GJ
		        ON FG.FIX_CODE=GJ.FIX_CODE
		            JOIN REVIEW R
		            ON R.JOIN_CODE=GJ.JOIN_CODE
		WHERE G.GR_CODE = #{starCode}
	</select>
	
	<select id ="NewGroupListAll" resultType="com.toleisure.mybatis.dto.GroupDTO">
		SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_NAME AS memName, CATE1 AS grCate1Name, CATE2 AS grCate2Name
		FROM NEWEST_GROUP_ALLVIEW
	</select>
	
	<select id ="HotHostList" resultType="com.toleisure.mybatis.dto.GroupDTO">
		SELECT MEM_PIC AS memPic, MEM_NAME AS memName, MEM_INTRO AS memIntro
		FROM HOT_HOST_VIEW
		
		
	</select>
	
	<select id ="HotHostListAll" resultType="com.toleisure.mybatis.dto.GroupDTO">
		SELECT MEM_PIC AS memPic, MEM_NAME AS memName, MEM_INTRO AS memIntro
		FROM HOT_HOST_AllVIEW
	</select>
	
	<select id ="ClosingGroupList" resultType="com.toleisure.mybatis.dto.GroupDTO">
		SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_NAME AS memName, CATE1 AS grCate1Name, CATE2 AS grCate2Name
		FROM CLOSING_VIEW
		
		SELECT AVG(R.REVIEW_STAR) AS grStarAvg
		FROM NOW_GROUP NG JOIN TBL_GROUP G
		ON NG.GR_CODE = G.GR_CODE
		    JOIN FIXED_GROUP FG
		    ON NG.NG_CODE=FG.NG_CODE
		        JOIN GR_JOIN GJ
		        ON FG.FIX_CODE=GJ.FIX_CODE
		            JOIN REVIEW R
		            ON R.JOIN_CODE=GJ.JOIN_CODE
		WHERE G.GR_CODE = #{starCode}
	</select>
	
	<select id ="ClosingGroupListAll" resultType="com.toleisure.mybatis.dto.GroupDTO">
		SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_NAME AS memName, CATE1 AS grCate1Name, CATE2 AS grCate2Name
		FROM CLOSING_GROUP_ALLVIEW
	</select>
	
	<select id ="RecommendGroupList" resultType="com.toleisure.mybatis.dto.GroupDTO">	
		<!-- SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_NAME AS memName, CATE1 AS grCate1Name, CATE2 AS grCate2Name
		FROM CLOSING_VIEW -->
		<!-- SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_ID AS memId, NG_MAX AS ngMax
		FROM RECOMMEND_VIEW
		WHERE MEM_ID = 'lee0528kr@naver.com' -->
		SELECT E.ngPic AS ngPic,E.grName AS grName,E.memName AS memName,E.grCate1Name AS grCate1Name,E.grCate2Name AS grCate2Name
		FROM
		(
		SELECT NG.NG_PIC AS ngPic,G.GR_NAME AS grName,M.MEM_NAME AS memName,C1.CATE_NAME AS grCate1Name,C2.CATE_NAME AS grCate2Name
		FROM MEMBER M JOIN TBL_GROUP G
		     ON M.CATE_CODE1=G.CATE_CODE1 OR  M.CATE_CODE1=G.CATE_CODE2 OR  M.CATE_CODE2=G.CATE_CODE1 OR  M.CATE_CODE2=G.CATE_CODE2
		     JOIN CATEGORY C1 
		     ON C1.CATE_CODE=G.CATE_CODE1
		     JOIN CATEGORY C2
		     ON C2.CATE_CODE=G.CATE_CODE2
		     JOIN NOW_GROUP NG
		     ON NG.GR_CODE = G.GR_CODE
		WHERE M.MEM_ID=#{memId}
		)E
		WHERE 3>=ROWNUM
		
		SELECT AVG(R.REVIEW_STAR) AS grStarAvg
		FROM NOW_GROUP NG JOIN TBL_GROUP G
		ON NG.GR_CODE = G.GR_CODE
		    JOIN FIXED_GROUP FG
		    ON NG.NG_CODE=FG.NG_CODE
		        JOIN GR_JOIN GJ
		        ON FG.FIX_CODE=GJ.FIX_CODE
		            JOIN REVIEW R
		            ON R.JOIN_CODE=GJ.JOIN_CODE
		WHERE G.GR_CODE = #{starCode}
	</select>
	
	<select id ="RecommendGroupListAll" resultType="com.toleisure.mybatis.dto.GroupDTO">	
		<!-- SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_NAME AS memName, CATE1 AS grCate1Name, CATE2 AS grCate2Name
		FROM CLOSING_VIEW -->
		<!-- SELECT NG_PIC AS ngPic, GR_NAME AS grName, MEM_ID AS memId, NG_MAX AS ngMax
		FROM RECOMMEND_VIEW
		WHERE MEM_ID = 'lee0528kr@naver.com' -->
		SELECT E.ngPic AS ngPic,E.grName AS grName,E.memName AS memName,E.grCate1Name AS grCate1Name,E.grCate2Name AS grCate2Name
		FROM
		(
		SELECT NG.NG_PIC AS ngPic,G.GR_NAME AS grName,M.MEM_NAME AS memName,C1.CATE_NAME AS grCate1Name,C2.CATE_NAME AS grCate2Name
		FROM MEMBER M JOIN TBL_GROUP G
		     ON M.CATE_CODE1=G.CATE_CODE1 OR  M.CATE_CODE1=G.CATE_CODE2 OR  M.CATE_CODE2=G.CATE_CODE1 OR  M.CATE_CODE2=G.CATE_CODE2
		     JOIN CATEGORY C1 
		     ON C1.CATE_CODE=G.CATE_CODE1
		     JOIN CATEGORY C2
		     ON C2.CATE_CODE=G.CATE_CODE2
		     JOIN NOW_GROUP NG
		     ON NG.GR_CODE = G.GR_CODE
		WHERE M.MEM_ID=#{memId}
		)E
		WHERE 10>=ROWNUM
	</select>
	
	<!-- ============================================테마별 DAO END================================================ -->
	
	
	<!-- <select id ="AvgStar" resultType="com.toleisure.mybatis.dto.GroupDTO">	
	SELECT AVG(R.REVIEW_STAR) AS REVIEW_STAR_AVG
	FROM NOW_GROUP NG JOIN TBL_GROUP G
	ON NG.GR_CODE = G.GR_CODE
	    JOIN FIXED_GROUP FG
	    ON NG.NG_CODE=FG.NG_CODE
	        JOIN GR_JOIN GJ
	        ON FG.FIX_CODE=GJ.FIX_CODE
	            JOIN REVIEW R
	            ON R.JOIN_CODE=GJ.JOIN_CODE
	WHERE G.GR_CODE = #{starCode};
	</select> -->
</mapper>