<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.toleisure.mybatis.dao.IMemberDAO">
	
	<select resultType="java.lang.String" id="efind">
		SELECT MEM_ID AS memId
		FROM MEMBER
		WHERE MEM_NAME = #{memName} AND MEM_TEL = #{memTel}
	</select>
	
	
   <select id="selectEmp" parameterType="com.toleisure.mybatis.dto.MemberDTO" resultType="String">
   		SELECT CASE WHEN COUNT(*) >= 1 THEN 'Y' ELSE 'N' END AS ISEMP
		FROM MEMBER
		WHERE MEM_NAME = #{memName} AND MEM_TEL = #{memTel}
   </select>
   
   
   <select id="login" parameterType="com.toleisure.mybatis.dto.MemberDTO" resultType="String">
   		SELECT CASE WHEN COUNT(*) >= 1 THEN 'Y' ELSE 'N' END AS ISEMP
		FROM MEMBER
		WHERE MEM_ID = #{memId} AND MEM_PW = #{memPw}
   </select>
   
</mapper>